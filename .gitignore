/my-notes.md
# The Phase 0 benchmark is local-only. It holds a scraped third-party catalog
# and a live Gemini key (benchmark/.env), so none of it belongs in a public
# repo. The measured conclusions live in specs/pre-build-validation/.
/benchmark/
__pycache__/
*.pyc

# Dev-store seed data (generated by app/scripts/pull-store-catalog.ts; import manually)
seed-products.csv
product_template.csv

# Never commit local SSH keys
ssh-key.key
ssh-key.pub
*.pem
